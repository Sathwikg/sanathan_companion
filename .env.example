# Copy to .env (git-ignored) and fill in before `docker compose up`.
# ---------------------------------------------------------------------------

# Host port the whole app is served on -> http://localhost:8080
WEB_PORT=8080

# --- Database (Supabase) ---------------------------------------------------
#
# IMPORTANT: do NOT use the "Direct connection" host
# (db.<project-ref>.supabase.co). Supabase publishes only an IPv6 address for
# it, and Docker's default bridge network is IPv4-only, so the container fails
# with "Network is unreachable".
#
# Use the SESSION POOLER instead — it has an IPv4 address:
#   Supabase Dashboard -> Project Settings -> Database -> Connection string
#   -> "Session pooler" (port 5432)
#
# Two things differ from the direct string: the host becomes
# aws-<n>-<region>.pooler.supabase.com and the username gains the project ref
# (postgres.<project-ref> instead of plain postgres).
#
# Session pooler (port 5432) is the right mode here — EF Core runs migrations
# on startup, and the transaction pooler on 6543 breaks prepared statements.
#
# If the password contains a $, double it ($$) — Compose expands .env values.
# SSL Mode matters more than it looks. In Npgsql, Require means "refuse to connect
# without TLS" and nothing else — no certificate chain check, no hostname check —
# so on a public-internet hop it stops eavesdropping but not impersonation. Only
# VerifyFull does both. (Trust Server Certificate is inert in Npgsql 10; the
# property is obsolete and documented as doing nothing.)
#
# Download the CA from your Supabase project settings, save it as the path in
# SUPABASE_CA_PATH below, and Compose will mount it into the API container.
#
# For a first-boot smoke test only, drop the Root Certificate and use plain
# `SSL Mode=Require` — but know that it accepts any certificate at all.
CONNECTION_STRING=Host=aws-0-<region>.pooler.supabase.com;Port=5432;Database=postgres;Username=postgres.<project-ref>;Password=YOUR-PASSWORD;SSL Mode=VerifyFull;Root Certificate=/etc/ssl/supabase/prod-ca.crt

# Where your copy of the Supabase CA lives on this machine. Mounted read-only at
# the path the connection string above names. Not committed — it is yours, it
# expires, and baking one into an image only moves the problem.
SUPABASE_CA_PATH=./deploy/certs/prod-ca.crt

# --- Auth ------------------------------------------------------------------
# JWT signing key. Must be random, at least 32 bytes, and must not contain
# "change-me" — the API refuses to start otherwise, which is deliberate.
#   PowerShell:  [Convert]::ToBase64String((1..48 | % { Get-Random -Max 256 }))
#   bash:        openssl rand -base64 48
JWT_SECRET=change-me-generate-a-random-48-byte-secret
