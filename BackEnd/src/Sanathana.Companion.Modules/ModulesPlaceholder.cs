namespace Sanathana.Companion.Modules;

/// <summary>
/// Reserved placeholder project. Kept to mirror the reference Clean Architecture folder
/// layout (Api / Application / Domain / Infrastructure / Modules). The working feature
/// pattern is the layered Domain/Application/Infrastructure/Api stack; future self-contained
/// feature modules can live here. Not referenced by the API.
/// </summary>
internal static class ModulesPlaceholder
{
}
