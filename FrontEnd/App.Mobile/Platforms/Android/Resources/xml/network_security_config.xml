<?xml version="1.0" encoding="utf-8"?>
<!--
    Cleartext HTTP is denied everywhere except the loopback hosts a Debug build uses:
    10.0.2.2 is the development machine as seen from the Android emulator, and localhost covers a
    physical device with `adb reverse tcp:7050 tcp:7050`.

    The previous manifest set android:usesCleartextTraffic="true" unconditionally, which shipped in
    Release too even though the deployed API is HTTPS. This keeps the developer convenience without
    weakening the released app.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
